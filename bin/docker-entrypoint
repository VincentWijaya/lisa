#!/bin/bash -e

# Catch if the command contains 'server' or 'thrust'
if [[ "$*" == *"server"* ]] || [[ "$*" == *"thrust"* ]]; then
  echo "Preparing database..."
  ./bin/rails db:prepare
fi

exec "${@}"
